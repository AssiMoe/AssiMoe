name: Build LibreMirror 2.0

on:
  push:
    branches:
      - libremirror-v2
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - name: Prepare Android SDK
        run: |
          SDKMANAGER="$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager"
          yes | "$SDKMANAGER" --licenses >/dev/null || true
          "$SDKMANAGER" "platforms;android-35" "build-tools;35.0.0"

      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Lint phone and Wear
        run: gradle --no-daemon :app:lintRelease :wear:lintRelease

      - name: Build phone and Wear APKs
        run: gradle --no-daemon :app:assembleRelease :wear:assembleRelease

      - name: Verify APK signatures
        run: |
          "$ANDROID_HOME/build-tools/35.0.0/apksigner" verify --verbose --print-certs app/build/outputs/apk/release/app-release.apk
          "$ANDROID_HOME/build-tools/35.0.0/apksigner" verify --verbose --print-certs wear/build/outputs/apk/release/wear-release.apk

      - uses: actions/upload-artifact@v4
        with:
          name: LibreMirror-2.0.0-phone
          path: app/build/outputs/apk/release/app-release.apk
          if-no-files-found: error

      - uses: actions/upload-artifact@v4
        with:
          name: LibreMirror-2.0.0-wear
          path: wear/build/outputs/apk/release/wear-release.apk
          if-no-files-found: error
